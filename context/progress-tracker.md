# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- `09-share-dialog.md` — complete. The workspace `Share` button now opens a real share dialog (styled after the provided reference: a top "Workspace link" card with a Copy-link button, a mail-icon invite field, then a "People with access" list with an "N total" count). The list shows the **owner** (with an `OWNER` badge) plus every collaborator. Owners can invite by email, remove collaborators, and copy the project link (temporary `Copied!` feedback); collaborators see the list read-only. Every member is enriched with Clerk display name + avatar (identifier fallback when no Clerk data). Backend collaborator API added (list / invite / remove) with server-side ownership enforcement on invite + remove. No local user table added — collaborators remain the `ProjectCollaborator` rows keyed by email; the owner comes from `Project.ownerId`.

## Current Goal

- Next feature-spec unit in sequence (`10`+). No active work in flight.

## Completed

- `01-design-system.md` — shadcn/ui (Base UI flavor, Nova preset) installed and configured; components added: Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea; lucide-react installed; `lib/utils.ts` cn() helper created by the CLI; `app/globals.css` rewritten so the dark palette from `ui-context.md` drives shadcn's semantic tokens; `dark` class added to `<html>` in `app/layout.tsx`. Verified with `next build` + a temporary preview route rendered in a real browser.
- `02-editor-chrome.md` — `components/editor/editor-navbar.tsx` (fixed-height top navbar, left/center/right sections, sidebar toggle button swapping `PanelLeftOpen`/`PanelLeftClose` based on `isSidebarOpen`, dark `bg-surface` background with `border-b border-surface-border`) and `components/editor/project-sidebar.tsx` (fixed-position floating panel — does not push page content — that slides in/out via a `translate-x` transition, `Projects` header with close button, shadcn `Tabs` for "My Projects" / "Shared" each with an empty placeholder state, full-width `New Project` button with `Plus` icon). Both are controlled components (`isOpen`/`isSidebarOpen` + callback props); the parent that wires shared state together is deferred to `08-editor-workspace-shell.md`. The dialog pattern requirement (title/description/footer, token-driven styling, no real dialogs yet) is already satisfied by the existing `components/ui/dialog.tsx` from `01-design-system.md` — its `DialogTitle`/`DialogDescription`/`DialogFooter` already resolve through the semantic tokens mapped in `globals.css`, so no new code was needed there. Verified with `next build`, lint, and a temporary preview route (open/closed states, tab switching) rendered in a real browser, then removed.
- `03-auth.md` — `@clerk/ui` installed; `NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in` and `NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up` added to `.env.local` (previously missing, needed so `proxy.ts` and Clerk's own components can resolve the public auth paths from env rather than hardcoded strings). `proxy.ts` created at the project root (Next.js 16 renamed `middleware.ts` → `proxy.ts`) using `clerkMiddleware` + `createRouteMatcher` in protected-first mode: only `${NEXT_PUBLIC_CLERK_SIGN_IN_URL}(.*)` and `${NEXT_PUBLIC_CLERK_SIGN_UP_URL}(.*)` are public, everything else calls `auth.protect()`. `app/layout.tsx` wraps `<body>` with `ClerkProvider` using the `dark` theme from `@clerk/ui/themes`, with `appearance.variables` overridden to reference the app's existing CSS custom properties (`var(--accent-primary)`, `var(--bg-surface)`, `var(--text-primary)`, etc.) instead of hardcoded colors — `borderRadius` maps to `var(--radius-2xl)` (card-level radius) and `fontFamily` to `var(--font-geist-sans)`. `components/auth/auth-layout.tsx` is a shared server component implementing the two-panel spec: `lg:grid-cols-2` 50/50 grid with a left brand panel (hidden below `lg`) and a right panel centering the Clerk form. The left panel is an elevated `bg-surface` (distinct from the `bg-base` right side, with a `border-r`), laid out as a flex column with `justify-between`: a brand lockup at top (cyan `bg-brand` rounded-square logo mark with a `Ghost` lucide icon in `text-primary-foreground` + "Ghost AI"), a centered content block (a `text-4xl`/`xl:text-5xl` headline "Design systems at the speed of thought.", a tagline, and a three-item feature list where each row pairs a tokened icon in a `bg-elevated` rounded square — `Sparkles`/`Share2`/`FileText` in `text-brand` — with a title and one-line description), and a `text-copy-faint` copyright footer at the bottom. No gradients or bordered feature cards; fits the viewport (no scroll). `app/sign-in/[[...sign-in]]/page.tsx` and `app/sign-up/[[...sign-up]]/page.tsx` render `<SignIn />`/`<SignUp />` inside `AuthLayout`. `app/page.tsx` (`/`) is now an async Server Component that calls `auth()` and redirects to `/editor` when `isAuthenticated`, otherwise to `/sign-in` — `/editor` itself does not exist yet (it's built in `08-editor-workspace-shell.md`), so this redirect currently 404s for authenticated users until that unit lands; this is expected incremental state, not a defect. `components/editor/editor-navbar.tsx`'s right section now renders Clerk's built-in `<UserButton />` (unmodified/undecorated, per spec — no custom appearance beyond the global `ClerkProvider` theme). Verified with `npm run build`, and in a real browser via Playwright: `/`, `/editor` → 307 to `/sign-in` when signed out; `/sign-in`, `/sign-up` → 200; dark two-panel layout confirmed at 1440px and form-only layout confirmed at 400px; cross-links between sign-in/up resolve to local routes (confirming the env vars are wired correctly), not Clerk-hosted URLs. Follow-up UI pass (per screenshot direction): reworked the left panel from a bare wordmark/text list into the richer brand panel described above and set the page `<title>` to "Ghost AI". Confirmed via Playwright computed styles that the headline, Clerk card heading, and body all resolve to `Geist, "Geist Fallback"` — Geist was already applied correctly (the earlier "wrong font" impression was the unpolished old layout, not an actual font bug). Note: attempted `appearance.options`/`layout.socialButtonsVariant: "blockButton"` to stack the social buttons as in the screenshot, but neither key changed the rendering in `@clerk/nextjs` 7.5.14 (Clerk renders icon-style social buttons by default here); reverted rather than leave dead config, since the spec says not to heavily customize Clerk internals.

- `04-project-dialogs.md` — Editor Home + the three dialogs (Create/Rename/Delete), the `useProjectActions` hook, sidebar per-project rename/delete actions on mock data, and the mobile backdrop scrim. Files: `components/editor/editor-shell.tsx` (client `isSidebarOpen` owner, instantiates `useProjectActions`, wraps subtree in `ProjectActionsProvider`, renders `<ProjectDialogs/>`, adds the `md:hidden` z-20 scrim below the z-30 sidebar), `app/editor/page.tsx` (Server Component home), `lib/projects.ts` (`Project`/`ProjectRole` types, `slugify()`, `mockProjects`), `hooks/use-project-actions.ts` (dialog + form + loading + local-list state; mutations are client-only mocks), `components/editor/project-actions-context.tsx`, `components/editor/dialogs/{create,rename,delete}-project-dialog.tsx`, `components/editor/project-dialogs.tsx`, `components/editor/new-project-button.tsx`, and `components/editor/project-sidebar.tsx` (owned/shared lists from context; rename/delete actions on owned projects only). Verified with `tsc --noEmit`, lint, build.
- `05-prisma.md` — data layer. `prisma/models/project.prisma` holds `ProjectStatus` (`DRAFT`/`ARCHIVED`), `Project` (`id` cuid PK, `ownerId` Clerk user, `name`, optional `description`, `status` default `DRAFT`, optional `canvasJsonPath`, `createdAt`/`updatedAt`, `@@index([ownerId])` + `@@index([createdAt])`) and `ProjectCollaborator` (`id` cuid PK, `projectId` + `project` relation with `onDelete: Cascade`, `email`, `createdAt`, `@@unique([projectId, email])`, `@@index([email])` + `@@index([projectId, createdAt])`). Multi-file schema enabled by pointing `prisma.config.ts` `schema` at the `prisma` directory (was `prisma/schema.prisma`); `prisma/schema.prisma` keeps only the `prisma-client` generator (output `app/generated/prisma`, already gitignored) + `postgresql` datasource. `lib/prisma.ts` is a `global`-cached singleton that branches on `DATABASE_URL`: `prisma+postgres://` → `new PrismaClient({ accelerateUrl })`, otherwise `new PrismaClient({ adapter: new PrismaPg({ connectionString }) })`. Migration `prisma/migrations/0_init/migration.sql` generated + applied. `prisma.config.ts` now loads `.env.local` first (then `.env`) via `dotenv` `config({ path: [...] })` — Next.js reads `.env.local` at runtime, so without this the CLI (which used bare `import "dotenv/config"` → `.env` only) would target a different DB than the app. Verified with `tsc --noEmit`, lint, `npm run build`, and runtime CRUD tests (create with nested collaborator, `DRAFT` default, cascade delete) through **both** branches: the direct `@prisma/adapter-pg` branch against a direct TCP port, and the **Accelerate branch** against a real Prisma Postgres cloud DB (`accelerate.prisma-data.net`).

- `06-project-apis.md` — backend-only project API. `app/api/projects/route.ts` (`GET` lists the authenticated user's own projects `where ownerId = userId`, `orderBy createdAt desc`; `POST` creates a project owned by the Clerk `userId`, defaulting a missing/blank name to `Untitled Project` and letting Prisma's `cuid()` supply the id — no sequential ids added, `201` on success). `app/api/projects/[projectId]/route.ts` (`PATCH` renames, `DELETE` removes — both owner-only). Shared boundary helpers live in `lib/projects-api.ts`: `readJsonBody()` (safe JSON parse → `{}` on malformed/non-object input), `normalizeName()` (trim → non-empty string or `null`, rejects non-strings), and `verifyProjectOwnership(projectId, userId)` returning a discriminated `{authorized:true} | {authorized:false, status:403|404}` (missing project → `404`, wrong owner → `403`, so a non-owner can't distinguish beyond the status code). Handler ordering per unit: auth (`401`) → ownership (`403`/`404`) → input validation (`400` on missing rename name) → mutation. **Middleware change (crosses into `03-auth.md`):** `proxy.ts` now excludes `/api/(.*)` from the protected-first `auth.protect()` via a new `isApiRoute` matcher, because `auth.protect()` answers unauthenticated requests with a `307` redirect to `/sign-in` — fine for page navigations but wrong for an API, which must return a JSON `401`. API routes now self-guard with `auth()` inside each handler (they already did), which is also the direction Clerk's own deprecation note recommends (per-route checks over centralized matching — see Open Questions). Page protection is unchanged: `/editor` etc. still `307` to sign-in when signed out. Verified: `npm run build`; unauthenticated `GET`/`POST`/`PATCH`/`DELETE` all return `401` (curl) while `/editor` still `307`s; and the full authenticated data path (create → cuid+`DRAFT` default, owner-scoped list with no cross-owner leak, `verifyProjectOwnership` owner/`403`/`404` decisions, rename, cascade-safe delete) exercised against the real Prisma Postgres DB via a throwaway `tsx` script.

- `08-editor-workspace-shell.md` — built the `/editor/[roomId]` workspace shell with server-side access checks; no canvas logic yet. **Access helpers** live in `lib/project-access.ts` (outside the page, per spec): `getClerkIdentity()` returns `{ userId, email }` (primary email via `currentUser()`, falling back to the first address; `null` when unauthenticated) and `getAccessibleProject(projectId, identity)` runs a single `findFirst` with `id` + `OR: [{ ownerId }, {collaborators.some.email}]` and a narrow `select: { id, name }` — returning `null` for **both** a missing project and one the identity can't access, so the two cases collapse to the same result and a non-member can't distinguish them. `app/editor/[roomId]/page.tsx` is an **async Server Component** (Next 16 `params` is a `Promise`): resolves `roomId`, then unauthenticated → `redirect("/sign-in")`, no accessible project → returns `<AccessDenied/>` (covers missing **and** unauthorized), otherwise fetches the sidebar's owned/shared lists via the existing `getEditorHomeProjects()` and renders `<EditorWorkspace/>` with the project name. `components/editor/access-denied.tsx` is a chrome-less server component: centered `flex-1` column, `Lock` icon in a `rounded-2xl bg-elevated` square, "Access denied" heading + short message, and a `Link` to `/editor` styled via `buttonVariants({ variant: "outline" })` (Base UI `Button` has no `asChild`, so the styled-Link pattern is used instead). **Workspace chrome** is a new client shell `components/editor/editor-workspace.tsx` (deliberately separate from the home `EditorShell`, whose `EditorNavbar` differs — see the pre-existing Architecture Decision): owns `isSidebarOpen` (default open) + `isAiSidebarOpen` (default closed), wraps the subtree in `ProjectActionsProvider` (so create/rename/delete + create-navigation still work inside a room) and renders `<ProjectDialogs/>`, the mobile scrim, both slide-over panels, and a central `bg-base` canvas placeholder in a `flex-1` `<main className="pt-14">`. `components/editor/workspace-navbar.tsx` is the room navbar (fixed h-14, `grid-cols-[1fr_auto_1fr]`): left = project-sidebar toggle (`PanelLeftOpen/Close`, kept so the overlay stays re-openable), center = truncating project name, right = a (visual-only, no behavior yet) `Share` outline button + a `Sparkles` AI-sidebar toggle that reflects open state via `aria-pressed` and an active `bg-elevated text-ai-text` class. `components/editor/ai-sidebar.tsx` mirrors the left sidebar's floating-overlay treatment on `right-3` (same navbar-height-derived top/height tokens, `translate-x` slide) with an "AI Assistant" header and a "coming soon" placeholder body. **Current-room highlight:** `useProjectActions` now also returns `activeRoomId` (already computed from `useParams<{roomId?}>()`); `ProjectSidebar` passes `isActive={project.id === activeRoomId}` to `ProjectItem`, which renders the active row with `bg-accent-dim` + `text-brand` and `aria-current="page"` (on the home route `roomId` is undefined → no highlight, unchanged). Verified with `tsc --noEmit`, `npm run lint`, and `npm run build` (all clean; `/editor/[roomId]` registered as a dynamic route). Note: real canvas, Liveblocks, AI chat, and sharing behavior are explicitly out of scope for this unit. **Follow-up fix:** the sidebar project row never navigated (only rename/delete were wired back in `07`), so clicking a project did nothing despite `04`'s "open an existing one / choose a project from the sidebar" copy. `ProjectItem`'s name/slug block is now a `next/link` `<Link href={/editor/${project.slug}}>` (slug === project.id === roomId) filling the `flex-1` area with a focus-visible ring; the rename/delete buttons remain siblings outside the link so their clicks don't navigate. **Follow-up (layout — docked panels):** the original shell placed both side panels as pure `position: fixed` floating overlays layered *over* a full-width canvas, so on desktop the open project sidebar covered the canvas's left edge and the canvas centered against the whole viewport rather than the space between the panels — the spec's "canvas fills the remaining space" was not really met. Reworked to a true docked layout: `ProjectSidebar`/`AiSidebar` gained a `docked` prop (default `false` keeps the `/editor` home's overlay behavior untouched) that, at `md+`, switches them from `fixed` to in-flow flex columns (`md:relative`, height auto to stretch the row) and collapses their footprint when closed via a negative margin (`md:-mr-80`/`md:-ml-80`) while retaining `w-80` so panel content never squishes mid-animation; below `md` they stay the original slide-over overlays with the scrim. `EditorWorkspace` now wraps `[sidebar · canvas · AI panel]` in a padded flex **row** (`p-3`, top padding derived from `--editor-navbar-height`) so the canvas (`flex-1`, now a bordered `rounded-2xl` card) genuinely fills the space between the two docked cards. Verified with `tsc --noEmit` (clean).

- `07-wire-editor-home.md` — wired the editor home to the real API and removed the mock data path. `app/editor/page.tsx` is now an **async Server Component** that calls the new `getEditorHomeProjects()` (`lib/projects-data.ts`) and passes `ownedProjects`/`sharedProjects` down to `EditorShell` — no client-side fetch for the initial load (invariant 4 / "server component" per spec). `lib/projects-data.ts` is the server-side data helper the spec calls for: it resolves owned projects `where ownerId = userId` and shared projects `where collaborators.some.email = <clerk email>` (email read via `currentUser()` — primary address, falling back to the first), both `orderBy createdAt desc`, and maps each Prisma row to the sidebar's `Project` UI shape via `toUiProject()` (sets `role` and `slug: project.id`, since the id *is* the room id). `hooks/use-project-actions.ts` was rewritten from a mock-list owner into a real API driver: it no longer holds a project array (lists come in as props from the server) and instead performs `fetch` mutations then re-reads server state. **Create:** `openCreate()` seeds a stable short suffix (`Math.random().toString(36).slice(2,8)`); `roomId` is derived reactively as `${slugify(name) || "project"}-${suffix}`; `submitCreate()` `POST`s `{ id: roomId, name }` and on success `router.push('/editor/'+roomId)`. **Rename:** `PATCH /api/projects/[id]` then `router.refresh()`. **Delete:** `DELETE /api/projects/[id]` then, if the deleted id equals the active room id (from `useParams<{roomId?}>()`), `router.push('/editor')`, else `router.refresh()`. All three guard against double-submit and `console.error` + keep the dialog open on a non-ok response. **API change (crosses into `06-project-apis.md`):** `POST /api/projects` now accepts an optional client-supplied `id` (validated by the new `normalizeId()` in `lib/projects-api.ts`) so the DB primary key stays aligned with the Liveblocks room id per the spec's "project ID and room ID should stay aligned"; a duplicate id is caught (`P2002`) and returned as `409` instead of a `500`. When `id` is absent the route still falls back to Prisma's `cuid()`. Wiring updates: `EditorShell` takes `ownedProjects`/`sharedProjects` props and forwards them into the hook; `project-dialogs.tsx` and `create-project-dialog.tsx` now pass/show `roomId` (the create dialog's preview line reads "Room ID: …") instead of the old bare slug; `mockProjects` was deleted from `lib/projects.ts` (only `slugify` + the `Project`/`ProjectRole` types remain). Verified with `tsc --noEmit`, `npm run lint`, and `npm run build` (all clean). Note: `/editor/[roomId]` does not exist until `08-editor-workspace-shell.md`, so a freshly-created project navigates to a 404 for now — expected incremental state, same pattern as the `/` redirect in `03`.

- `09-share-dialog.md` — project sharing. **Backend:** `lib/collaborators.ts` holds the enrichment layer — `MemberView` (`{id,email,name,imageUrl,role:"owner"|"collaborator"}`), `normalizeEmail()` (trim + lowercase + minimal shape check at the boundary; lowercased because Clerk stores emails lowercased, so stored collaborator rows keep matching the identity email used in `getAccessibleProject`/`getEditorHomeProjects` access checks), and `listProjectMembers({id, ownerId})` which reads `ProjectCollaborator` rows (ordered `createdAt asc`) and, **in parallel**, resolves the owner (Clerk `getUser(ownerId)`) and enriches collaborators (`getUserList({ emailAddress })`, keyed by lowercased email). It returns the **owner first (role `owner`)** then collaborators (role `collaborator`) so the dialog renders one "people with access" list. Name = firstName+lastName → username → `null`; avatar = `imageUrl` only when `hasImage` (Clerk's generated default avatars are treated as "no avatar"); owner email = primary (falling back to first). Enrichment is best-effort: a Clerk failure is caught + logged and members fall back to their identifier (email, or "Project owner"). **API routes:** `app/api/projects/[projectId]/collaborators/route.ts` — `GET` (membership-gated: owner **or** collaborator via the existing `getAccessibleProject`, so a non-member gets `404`) returns `{ members }`; `POST` (owner-only via `verifyProjectOwnership`) invites by email, `400` on invalid email, `409` on `P2002` duplicate, returns the refreshed `{ members }` (`201`). `app/api/projects/[projectId]/collaborators/[collaboratorId]/route.ts` — `DELETE` (owner-only) removes via `deleteMany({ id, projectId })` (project-scoped so a foreign collaborator id can't be removed), `404` when nothing matched, returns refreshed `{ members }`. POST/DELETE build members with `ownerId: userId` (owner-only routes, so the caller is the owner). Both invite + remove enforce ownership server-side per spec. Shared helper `ownershipErrorResponse(403|404)` was hoisted into `lib/projects-api.ts` (and the existing `[projectId]/route.ts` refactored to use it) to avoid duplicating the `403`/`404` JSON shape. `lib/project-access.ts` `getAccessibleProject` now also selects `ownerId` (interface `AccessibleProject` gained `ownerId`) so the page can compute `isOwner = project.ownerId === identity.userId` and the GET route can pass the project straight into `listProjectMembers`. **UI:** `components/editor/share/share-dialog.tsx` is a `"use client"` dialog modeled on the provided reference — an owner-only "Workspace link" card (Copy-link button → `Copied!` for 2s via `navigator.clipboard`), an owner-only mail-icon invite field (raw `<input>` inside a bordered card so the icon sits inside), then a "People with access" header with an "N total" count and the member list. Each row is a bordered card: avatar, name (falling back to email → role label), a role badge on every row (brand `OWNER` pill `border-brand/30 bg-accent-dim text-brand`; muted gray `COLLABORATOR` pill `border-subtle-border text-copy-muted` — a label only, since role editing isn't built yet), email sub-line when a name is present, and a remove (Trash2) button only on collaborator rows when `isOwner`. Collaborators see only the read-only list (no link card, no invite, no remove). The dialog fetches on open and manages its own loading/invite/remove/copy/error state. Avatars render a Clerk `<img>` when available, else an initial in a `bg-subtle` circle (plain `<img>` with a scoped eslint-disable — Clerk avatar hosts aren't configured for `next/image`). `DialogContent` is widened to `sm:max-w-xl`. Wiring: `EditorWorkspace` gained `projectId`/`isOwner` props + an `isShareOpen` state and renders `<ShareDialog>`; `WorkspaceNavbar` gained an `onOpenShare` prop wired to the existing `Share` button; `app/editor/[roomId]/page.tsx` passes `projectId`/`isOwner`. Verified with `tsc --noEmit`, `npm run lint`, `npm run build` (both new routes registered), unauthenticated `GET`/`POST`/`DELETE` → `401` via curl, and a throwaway `tsx` script against the real DB + Clerk: `listProjectMembers` returns owner-first (role `owner`, email resolved) + collaborator (role `collaborator`, email fallback when unmatched), member count correct, cascade delete cleans up. Note: the read-only vs owner UI branch was verified structurally (build/lint/data path), not in a live signed-in browser session.

## Next Up

- `10`+ per the feature-specs sequence.

## Open Questions

- `DATABASE_URL` now points at a real Prisma Postgres cloud DB (`prisma+postgres://accelerate.prisma-data.net/...` in `.env.local`); the `0_init` migration is applied there and the Accelerate branch is verified end-to-end. (Earlier friction, resolved: the local `prisma dev` emulator — Postgres-wire over SQLite — rejected `migrate dev` on the shadow-DB step with `P1017` and refused HTTP/Accelerate queries for client 7.8.0. The migration was authored via `prisma migrate diff --from-empty --to-schema prisma --script` and applied with `prisma migrate deploy`, which is the flow that also works against the cloud DB.)

- Clerk (`@clerk/nextjs` 7.5.14) logs a deprecation warning at runtime: `createRouteMatcher` is deprecated in favor of resource-based (per-route) auth checks instead of centralized middleware path matching — see https://clerk.com/docs/guides/development/upgrading/upgrade-guides/migrate-from-create-route-matcher. `03-auth.md` explicitly specified the `proxy.ts` + route-matcher pattern used here, so it was kept as specified; revisit if a future unit wants to migrate to per-route `auth.protect()` calls instead. Update (`06-project-apis.md`): `/api/(.*)` is now excluded from the centralized `auth.protect()` and each API handler enforces `auth()` itself (returning `401`) — a first, partial step in the recommended per-route direction, taken because APIs must answer with a status code rather than the redirect `auth.protect()` produces. Page routes still use the centralized middleware protection.

## Architecture Decisions

- shadcn CLI (v4.13.0) defaults to Base UI (`@base-ui/react`) rather than Radix UI as the primitive layer, and to a "Nova" preset (Lucide icons + Geist fonts) — matches this project's existing icon/font choices, so kept the default instead of forcing `--base radix`.
- The dark palette from `ui-context.md` is defined as raw CSS custom properties (`--bg-base`, `--text-primary`, etc.) in `app/globals.css`, then mapped onto shadcn's semantic variables (`--background`, `--card`, `--primary`, ...) so generated `components/ui/*` files stay untouched and still render on-theme.
- `--color-base` was deliberately NOT registered as a `@theme inline` color token: Tailwind's built-in `--text-base` font-size scale key collides with any color named "base", which silently turned every `text-base` (font-size) utility into a text-color rule and made card titles nearly invisible. Fixed by declaring `bg-base` as a standalone `@utility` instead. Keep this in mind before adding more single-word token names — check for collisions with Tailwind's reserved scale keys (`base`, `sm`, `lg`, `xl`, etc.) first.
- `EditorNavbar` and `ProjectSidebar` are controlled components (open/close state lives in a future parent), not self-managing — this keeps them reusable once `08-editor-workspace-shell.md` composes the full editor shell and adds more sidebar/navbar consumers.
- That parent is `components/editor/editor-shell.tsx`, introduced by the editor-home slice of `04-project-dialogs.md`. It is the single `"use client"` boundary for the editor chrome: it holds `isSidebarOpen` and takes `children`, so route pages (`app/editor/page.tsx`, and later `/editor/[roomId]`) stay Server Components and can do server-side data fetching — satisfying invariant 4 (client components only where interactivity requires them). It was deliberately NOT put in an `app/editor/layout.tsx`: `08-editor-workspace-shell.md` gives `/editor/[roomId]` a different navbar (project name, share button, AI sidebar toggle), so a shared layout-level navbar would have to be branched per-route. Sidebar state resets across an `/editor` → `/editor/[roomId]` navigation as a result; if that becomes undesirable, lift it to a layout with a route-aware navbar rather than duplicating the shell.
- `EditorShell` uses `flex flex-1 flex-col` (not `min-h-screen`) because `app/layout.tsx` already sets `html.h-full` + `body.min-h-full flex flex-col`, and `<main>` carries `pt-14` to clear the `fixed` h-14 navbar. No background class is set on the shell — `globals.css`'s base layer already applies `bg-background` (→ `--bg-base`) to `body`.
- `ClerkProvider`'s `appearance.theme` is Clerk's `dark` theme (from `@clerk/ui/themes`), not the `shadcn` theme, even though `components.json` is present — `03-auth.md` explicitly specified "dark theme as base + override variables with app CSS custom properties" rather than the shadcn theme. All Clerk color/typography/radius variables are pointed at this project's existing tokens (`var(--accent-primary)`, `var(--bg-surface)`, `var(--radius-2xl)`, `var(--font-geist-sans)`, ...) so the two approaches converge on the same visual result without pulling in `@clerk/ui/themes/shadcn.css`.
- `NEXT_PUBLIC_CLERK_SIGN_IN_URL` / `NEXT_PUBLIC_CLERK_SIGN_UP_URL` were added to `.env.local` (they didn't exist before `03-auth.md`) so `proxy.ts` can derive its public-route matcher from env rather than hardcoded path strings, per spec.

## Session Notes

- Next.js App Router treats any `app/` folder prefixed with `_` as a private folder excluded from routing entirely — a temporary preview route must NOT be underscore-prefixed or it 404s silently. Use a plain folder name (e.g. `app/preview-x/`) and delete it after verifying in the browser.
- The Playwright MCP browser is a single shared Chrome instance keyed by a fixed user-data-dir; if a prior session left it open (idle on `about:blank`), new `browser_navigate` calls fail with "Browser is already in use" until that stray process is killed.
